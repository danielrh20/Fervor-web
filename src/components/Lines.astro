<div class="fixed w-full h-full inset-0 bg-container">
</div>

<style>
  .bg-container {
    position: fixed;
    width: 100%;
    height: 100%;
    inset: 0;
    overflow: hidden;
  }

  /* Imagen de fondo */
  .bg-container::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: url('/fondo-fervor.png');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    opacity: 0.4;
    z-index: 1; /* Para que esté debajo del granulado */
  }

  /* Granulado animado */
  .bg-container::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image: url('https://www.transparenttextures.com/patterns/noise.png'); /* Patrón de ruido */
    opacity: 0.2;
    z-index: 2; /* Encima de la imagen de fondo */
    animation: grain-animation 0.5s steps(10) infinite;
  }

  /* Animación de ruido */
  @keyframes grain-animation {
    0% {
      transform: translate(0, 0);
    }
    100% {
      transform: translate(-10px, -10px);
    }
  }
</style>
